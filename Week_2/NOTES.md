
# 1st I have created a virtual environment names bavithraproject
# then I activated the virtual environment
# I install pip and install 'requests'
# Then I Deactived the venv

# I kept my cuuert folder and installed uv using pip install here uv is also a package like pip.
# uv is faster than pip
# Then I created venv with uv then the i start activated the venv it takes my current folder into venv 
# Then I type uv pip install requests
# Then i stored all its requirements in requirement.txt 

